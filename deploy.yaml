version: 2
default_flags:
  pull_policy: IfNotPresent
artifacts: []
flavors:
  default: {}
image_registries:
  - name: local
    driver: noop
    prefix: local-registry
  - name: gcp
    driver: gcp
    prefix: ***REMOVED***
