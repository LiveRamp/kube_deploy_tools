# This is a corresponding text fixture
# that represents the expected fixed configuration
# from a v1 configuraiton
version: 2
default_flags:
  pull_policy: IfNotPresent
artifacts:
  - name: local-staging
    image_registry: local
    flags:
      target: local
      environment: staging
      cloud: local
      pull_policy: Always
  - name: colo-service-prod
    image_registry: aws
    flags:
      target: colo-service
      environment: prod
      cloud: colo
  - name: colo-service-staging
    image_registry: aws
    flags:
      target: colo-service
      environment: staging
      cloud: colo
  - name: us-east-1-prod
    image_registry: aws
    flags:
      target: us-east-1
      environment: prod
      cloud: aws
flavors:
  default: {}
image_registries:
  - name: local
    driver: noop
    prefix: local-registry
  - name: aws
    driver: aws
    prefix: ***REMOVED***
    config:
      region: us-west-2
  - name: gcp
    driver: gcp
    prefix: ***REMOVED***
hooks:
  - default
  - scripts/render_deploys_hook
