# This is a test fixture
deploy:
  clusters:
    - target: local
      environment: staging
    - target: colo-service
      environment: prod
    - target: colo-service
      environment: staging
  flavors:
    default: {}
  hooks:
    - default
    - scripts/render_deploys_hook
