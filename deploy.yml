deploy:
  clusters:
    - target: local
      environment: staging
      flags:
        cloud: local
        image_registry: local-registry
        pull_policy: IfNotPresent
    - target: colo-service
      environment: prod
      flags:
        cloud: colo
        image_registry: ''
        pull_policy: IfNotPresent
    - target: colo-service
      environment: staging
      flags:
        cloud: colo
        image_registry: ''
        pull_policy: IfNotPresent
  flavors:
    default: {}
image_registries:
  - name: aws
    driver: aws
    prefix: ***REMOVED***
    config:
      region: us-west-2
  - name: gcp
    driver: gcp
    prefix: ***REMOVED***
